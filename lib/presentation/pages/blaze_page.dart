import 'package:flutter/material.dart';

class BlazePage extends StatelessWidget {
  const BlazePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
